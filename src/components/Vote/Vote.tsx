import { VoteOption } from '@/app/types';
import { JSX } from 'react';

type Props = {
  options: VoteOption[];
};

export function Vote({ options }: Props): JSX.Element {
  return (
    <div>
      <div className=""></div>
    </div>
  );
}
