import { VoteOption as VoteOptionType } from '@/app/types';
import { JSX } from 'react';

export function VoteOption({ id, name, votesNumber }: VoteOptionType): JSX.Element {
  return (
    <div>
      <div className=""></div>
    </div>
  );
}
